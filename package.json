{
  "name": "popvox-ai-era",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "build": "node scripts/build-witness-pages.mjs && node scripts/build-event-pages.mjs && node scripts/build-bill-pages.mjs",
    "build:data": "node scripts/build-house-journal-data.mjs && node scripts/build-congressional-reports-data.mjs",
    "check": "for f in scripts/*.mjs *.js; do node --check \"$f\" || exit 1; done"
  }
}
